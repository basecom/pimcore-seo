services:

    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    SeoBundle\EventListener\XliffListener:
        tags:
            - { name: kernel.event_subscriber }
